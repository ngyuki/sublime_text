{
    "extensions": ["md"],

    // 保存時に行末の空白を削除する
    "trim_trailing_white_space_on_save": true,

    // ワードラップはソースコード以外なら自動的に有効
    "word_wrap": "auto",

    // Markdown MarkdownEditing
    "mde.list_indent_bullets": ["-", "-", "-"],
}
